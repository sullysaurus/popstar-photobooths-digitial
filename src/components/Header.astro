---
// Astro component frontmatter
---

<header data-name="header" class="fixed w-full z-50 bg-gradient-to-r from-pink-500 via-purple-500 to-pink-500 pt-safe">
    <div class="container mx-auto px-4 md:px-8">
        <div class="flex flex-col md:flex-row justify-between items-center py-3 md:py-4">
            <h1 data-name="logo" class="text-2xl md:text-4xl logo-font text-white drop-shadow-lg text-center md:text-left mb-3 md:mb-0">
                <i class="fas fa-camera-retro mr-3"></i>
                <span class="hidden md:inline">Popstar Photo Booths</span>
                <span class="inline md:hidden">Popstar Booths</span>
            </h1>
            <button 
                data-name="book-now-button" 
                onclick="document.querySelector('[data-name=contact-form]').scrollIntoView({ behavior: 'smooth' })"
                class="header-btn text-white px-6 md:px-8 py-2 md:py-3 rounded-full text-base md:text-lg font-semibold w-full md:w-auto">
                Book Now
            </button>
        </div>
    </div>
</header>

<style>
    .header-btn {
        background: rgba(255, 255, 255, 0.2);
        backdrop-filter: blur(5px);
        border: 1px solid rgba(255, 255, 255, 0.3);
        transition: all 0.3s ease;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }

    .header-btn:hover {
        background: rgba(255, 255, 255, 0.3);
        transform: translateY(-2px);
    }
</style>
